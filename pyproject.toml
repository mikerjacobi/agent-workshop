# Tooling config. The installable package lives in cli/.

[tool.mypy]
python_version = "3.12"
files = ["agents"]
strict = true
scripts_are_modules = true
follow_imports = "silent"
pretty = true

[tool.ruff]
line-length = 120
target-version = "py312"
