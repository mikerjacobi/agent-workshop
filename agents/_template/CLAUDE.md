# Layout

Your identity is at `SOUL.md`. Who you are helping is at `USER.md`.

Your skills are auto-discovered from your workspace (authored under `skills/` in the repo):

| Skill    | Use it for |
|----------|------------|
| `<name>` | ...        |

Invoke a skill with the `Skill` tool (`Skill(skill="<name>")`) to pull its
instructions into your context.

<!--
Delete this block if your agent declares no parameters.

## Runtime configuration

| Env var  | What it is |
|----------|------------|
| `<KEY>`  | ...        |

Read them with `$<KEY>`. Never print their values back to the user.
-->

Persistent scratch space lives under your working directory.
